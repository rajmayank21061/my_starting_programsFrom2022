#include<iostream>
using namespace std;

    int mn( int size)
     {
        for (int i=0; <=size; i++)
        {
            cout<<"printing the array"<< k[i]<<endl;
            findsize = sizeof(k[i])
        }
           cout<<"Puting done"<<endl;
           return findsize;
     }

int main()
{ 
    int k[] = {1,2,3,543,400}
    cout<<k[]<<endl;
    int sizint = sizeof(k) / sizeof(int)

     cout<<mn(sizing)<<endl;
     
   
    return 0;
}